import Notes from '../components/Notes';

const Home = () => {
  return (
    <div className="min-vh-100" style={{ background: 'var(--bg-primary)' }}>
      <Notes />
    </div>
  );
};

export default Home;